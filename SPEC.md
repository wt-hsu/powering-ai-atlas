# 台灣 AIDC 開發案地圖・製作規格書 v2

2026-08-25|狀態:**本 repo 為正本**,深色定稿依本規格由 `gen.js` 產出
取代:v1(2026-08-25 上傳稿,存於會話紀錄,僅供參考)
v1→v2 變更:深色科技感為母檔並以主題 token 同產淺色變體;上色階段(v1 §8)併入工程;NVIDIA 改 5/27 嵌套、100MW 降側卡;新增全域確定性散置與內湖收束框;雙語(標題/圖例/警語)納入本階段;資料查核紀錄入 repo(`DATA_AUDIT.md`)。

---

## 1. 訊息目的(先於一切樣式)

一句話:**需求最密的地方(北部),正是供電最緊的地方(北部限制區);成長的出口在南部算力廊帶。**
地圖不是案場名錄,是把「Z2 鉤子數字」空間化——政策圖層(北部斜線、南部廊帶)與案場泡泡同等重要,缺一就退化成房仲地圖。

**載體優先序**:全景圖 Z2b(正本、全寬嵌入)>文章版(同檔)>LinkedIn(裁切衍生版,可犧牲側卡與註腳,僅保留泡泡+斜線區+廊帶+一句警語)。不讓 2 秒載體反過來決定母檔資訊密度。

## 2. 資料(單一事實來源)

- 圖上一切標記由 **`sites.json`** 驅動;改數字改這裡,不改圖面。
- 每案來源、查核日期與可信度評級記錄於 **`DATA_AUDIT.md`**;兩檔同步維護,發佈前檢查 `audit_date`。
- **不入圖、列側卡**:AWS 台北區域(區位未揭露);中華電信 20 座 IDC 升級(面狀事實);NVIDIA×鴻海 100MW 三期規劃(2025 年宣布,2026 無更新,不畫外圈)。
- **排除**:Meta(僅傳聞,C 級);離島無案場,不入圖。
- **座標精度=鄉鎮級示意**,且密集區點位經散置(見 §5),警語必須同時聲明兩者。
- 2026-08-25 查核要點:NVIDIA×鴻海亞灣超算 5MW 集群已商轉(2026-07-31),首期 27MW 建置中,100MW 規劃停留在 2025 年資訊;台灣大×Vantage 確認營運中(開幕滿租、預售完售);Epoch ETW1 以 IT 22–23.5MW 記載,32MW 之說疑為總受電口徑。

## 3. 視覺編碼

- **圓面積 ∝ 容量 MW**:`r = max(5, 1.7 × √MW)`(px,@920 寬畫布)。圖例給 250/50/15 三階嵌套圓。
- **狀態語意由形狀承擔,顏色只作加強**(色盲安全的根):
  - 營運中=實心圓(強調色)+2px 底色分隔環+微光暈(≤2px,不參與面積感知)
  - 建置/規劃中=虛線圓(dash 4 3,stroke 1.5,淡強調色填充)
  - 規模未揭露=菱形(不參與面積比例——沒有數字就不給面積;此符號同時是「誠實原則」的可見示範)
- **已商轉首期→建置上限**(NVIDIA 5→27)=嵌套:內實心圈=已商轉、外虛線圈=建置中容量。
- **容量口徑混用警語必須上圖**:「圓面積≈公開揭露容量,口徑依個案(契約容量/最大負載/IT 負載)」。
- 文字一律用文字色階(ink/ink2/ink3),不得用系列色;數字/代碼用等寬字。

## 4. 主題與色彩(v1 §8 併入工程)

- **深色為母檔,淺色為變體**,同一 `gen.js` 以主題 token 產出,不維護兩份圖面程式。
- Token(核心值,完整見 `gen.js` `THEMES`):

| token | dark | light | 用途 |
|---|---|---|---|
| bg | `#0A101C` | `#F7F9FC` | 畫布 |
| land | `#18222F` | `#E9EDF3` | 陸地 |
| accent | `#00A3B8` | `#00829D` | 案場泡泡(唯一系列色) |
| corridor | `#BD8628` | `#A87415` | 南部廊帶(第二強調,註記用) |
| ink/ink2/ink3 | `#E9F0F8`/`#A9B8CC`/`#74869E` | `#14202E`/`#46566B`/`#74869E` | 文字三階 |

- 兩組 accent+corridor 均通過 dataviz 六項檢查(亮度帶、彩度下限、CVD 分離、正常視覺下限、對比;`validate_palette.js`,dark/light 各一次)。**改色必須重跑驗證**。
- 政策圖層用中性灰(斜線紋理、卡框),不用紅——它們是事實陳述,不是好壞判斷。
- 科技感手法白名單:深底、46px 網格、等寬數字(IBM Plex Mono)、≤2px 微光暈。**禁**:重 glow/霓虹(光暈膨脹視覺面積=對「面積∝MW」撒謊)、彩虹色。

## 5. 幾何、投影與散置

- 邊界:內政部縣市界,npm `taiwan-atlas`(counties-10t.json,1:10 萬,TWD97 經緯度)。縣市名用「台」(台北市)。
- 排除金門/連江/澎湖後:`geoTransverseMercator().rotate([-121,0]).fitExtent([[178,30],[500,645]])`。案場點用同一投影函式,保證對齊。
- **全域確定性散置**:所有點兩兩推開至圓緣間隙 ≥4px(含分隔環),質心守恆、迭代固定順序,重跑結果相同。跨聚落也算(例:台中點原始座標落在 Google 250MW 圓內)。散置量在產圖 log 輸出,典型 <22px。
- **內湖收束框**:3 案外接圓 +5px 虛線圈+「內湖 ×3」徽章。其他聚落散置後無完全重疊者不加框。
- 圖層堆疊(下→上):底色 → 網格 → 陸地 → 北北基桃斜線 pattern(45°,線距 6px)→ 縣市界 0.7px → 國界輪廓 1.1px → 廊帶虛線框 → 泡泡(**由大到小**)→ 收束框 → 卡片 → 導引線 → 圖例 → 註腳。

## 6. 標註系統(碰撞規則)

- 聚落收成一張卡:桃園 5 案、內湖 3 案、台南 2 案;單點區一卡一線(台南例外:一卡兩線分指兩圓)。
- 左欄(x 8–168)卡片對應地理由北而南;**例外**:彰化卡列於台中卡之前——兩點圖上相距僅 ~20px,此序使導引線不交叉、不穿 Google 大圓(緯度差 0.08°,讀者不可感)。
- 右欄:A 欄(x 566–734)內湖卡、政策註、側卡;B 欄(x 744–912)圖例、警語卡。
- 導引線=肘形折線(水平段後斜入),1px;端點停在目標圓緣外 3px;`elbowX` 可指定轉折點以繞開其他圓(台中線南繞 Google 圓即用此)。
- 卡內每列帶狀態圖徽(●/虛線○/◇/嵌套◉),與地圖語意一致。卡內文案行寬 ≤129px,超寬即改寫或折行——**溢出即 bug**。

## 7. 政策圖層(本圖的觀點所在)

- **北北基桃**=45° 斜線 pattern。註記:「北北基桃・5MW+ 暫緩供電區|2024 起實施(決策 2023 年底);2026-07 研議『電廠周邊算力專區』有條件鬆綁,未廢止」。
- **南臺灣算力廊帶**=圓角虛線框包住 南科—沙崙—高雄,標籤置框外右下,corridor 色。

## 8. 雙語與輸出

- 本階段雙語範圍:**標題、副題訊息句、圖例、警語**中英並排;卡片內文中文為主(案名本即外文)。LinkedIn 全英文版=之後的衍生工作,資料不變、僅換文案。
- 輸出(均由 `node gen.js` + `node shot.js` 產出):
  - `output/AIDC_map_{dark,light}.svg`:母檔 viewBox 920×660,文字保留向量(引用字體名,設計端需裝 Noto Sans TC + IBM Plex Mono)
  - `output/AIDC_map_{dark,light}.html`:自含 @font-face 預覽頁
  - `output/AIDC_map_{dark,light}@2x.png`:1840×1320
- 字體 vendor 於 `assets/fonts/`(OFL 授權,授權檔同目錄),PNG 輸出不依賴環境字體。
- LinkedIn 裁切 1200×627:自 PNG 裁切,右欄兩欄改一欄之重排屬衍生版工作,不改母檔。

## 9. 重現與維護

```bash
npm install
node gen.js    # sites.json → output/*.{svg,html},log 散置位移
node shot.js   # Chromium 截圖 → output/*@2x.png(需 playwright-core + chromium)
```

- 更新流程:改 `sites.json` → `node gen.js && node shot.js` → **目檢 PNG**(碰撞/溢出/導引線)→ 同步 `DATA_AUDIT.md` 對應列與 `audit_date`。
- 待核清單(升級對外版前):見 `DATA_AUDIT.md`「待核」欄——其中 NVIDIA 27MW 全量進度、Epoch 口徑為持續追蹤項。

## 10. 已知取捨

- 離島與 AWS 不入圖:寧可側卡誠實交代,不放猜測點位。
- 圓面積用「揭露容量」而非統一口徑:統一口徑不存在,警語上圖是唯一誠實解。因此**圖上不做任何容量加總**。
- 中華電 20 座既有 IDC 不畫點:面狀事實,畫點會與新建案混淆。
- NVIDIA 100MW 不畫外圈:2025 年規劃、2026 無更新,畫了等於替單一廠商的舊聞背書;側卡註記。
- 菱形類目僅崙坪一案仍保留:它是「沒數字就不給面積」原則的可見證據,教育價值大於圖例稅;若未揭露案增多,重審此決定。
- 散置犧牲點位精確換可讀性:警語聲明,且座標本就只承諾鄉鎮級。
